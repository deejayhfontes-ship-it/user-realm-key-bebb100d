# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Google Service Account credentials
fontesgraphics-*.json
*.service-account.json

# Configs locais com credenciais (NUNCA subir pro GitHub!)
src/config/trello.local.ts

# Environment variables (NUNCA subir pro GitHub!)
.env
.env.*
!.env.example

scripts/

# ============================================================
# PROTEÇÃO CONTRA VAZAMENTO DE CREDENCIAIS — NUNCA REMOVER!
# Adicionado após incidente de API key exposta no git (jun/2026)
# ============================================================

# Scripts de teste e análise (nunca subir)
*.cjs
*.mjs
test_*.ts
test_*.cjs
test_*.mjs
analisar-*.cjs
extrator-*.mjs
capturar-*.mjs
explorador-*.mjs
garimpar-*.cjs
injetor-*.mjs
buscar-*.mjs
salvar-*.mjs
teste-*.mjs

# Arquivos de sessão e dados capturados
*.session.json
orion-*.json
orion-*.html
resultado-gerado.*
teste-screenshot.*
upload_dist.*

# Pastas de pesquisa e raspagem (nunca subir)
nova-raspagem-definitiva/
novaraspagem/
research/

# Migrations com dados sensíveis (seeds com credenciais)
# REGRA: Migrations NUNCA devem conter API keys, passwords ou tokens
# Use apenas estrutura SQL pura (CREATE TABLE, CREATE POLICY, etc.)
supabase/migrations/*seed*.sql
supabase/migrations/*keys*.sql
supabase/migrations/*secret*.sql
supabase/migrations/*credential*.sql
